# Copy to ignored .dev.vars and supply your own values. Never commit that file.
SMTP_USERNAME=
SMTP_PASSWORD=
TURNSTILE_SECRET_KEY=
# Generate independently using: openssl rand -hex 32
RATE_LIMIT_SECRET=
