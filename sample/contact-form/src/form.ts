export function form(siteKey: string): string {
  const safeKey = siteKey.replace(/[&<>"']/g, c => `&#${c.charCodeAt(0)};`)
  return `<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Contact us</title>
<link rel="stylesheet" href="/form.css"><main><h1>Contact us</h1><p>Send a message to our team. We’ll reply by email.</p>
<form><label>Name<input name="name" maxlength="100" autocomplete="name" required></label><label>Email<input name="email" type="email" maxlength="254" autocomplete="email" required></label><label>Subject<input name="subject" maxlength="160" required></label><label>Message<textarea name="message" maxlength="8000" rows="7" required></textarea></label><div hidden aria-hidden="true"><label>Website<input name="website" tabindex="-1" autocomplete="off"></label></div><div class="cf-turnstile" data-sitekey="${safeKey}" data-action="contact"></div><button>Send message</button><p id="result" role="status" aria-live="polite"></p></form></main><script src="/form.js" defer></script><script src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer></script></html>`
}
export const css = `body{font:18px system-ui;background:#f5f6f8;color:#17202a;margin:0}main{max-width:38rem;margin:3rem auto;padding:2rem;background:white;border-radius:1rem}label{display:block;margin:1rem 0}input,textarea,button{box-sizing:border-box;font:inherit;width:100%;padding:.7rem;border:1px solid #82909c;border-radius:.4rem}button{background:#1647a2;color:white;cursor:pointer}button:disabled{opacity:.6}#result{min-height:3rem}`
export const script = `document.querySelector('form').addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget,button=form.querySelector('button'),result=document.querySelector('#result');button.disabled=true;result.textContent='Sending…';const data=Object.fromEntries(new FormData(form));data.turnstileToken=data['cf-turnstile-response'];delete data['cf-turnstile-response'];try{const response=await fetch('/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const reply=await response.json();if(reply.status==='accepted'){result.textContent='Your message was accepted for sending. Thank you!';form.reset()}else if(reply.status==='unknown'){result.textContent='We could not confirm the result. Please do not resend yet. Reference: '+reply.id}else{result.textContent=reply.error||'Unable to send. Please try later.';button.disabled=false}if(window.turnstile)window.turnstile.reset()}catch{result.textContent='We could not confirm the result. Please do not resend yet.'}});`
